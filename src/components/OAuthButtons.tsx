// src/app/components/OAuthButton.tsx

import Typography from '@mui/material/Typography';
import Container from '@mui/material/Container';

export const metadata = {title: "OAuthButton| Sandrapp"};

export default function OAuthButton() {

  return (
    <Container>
        <Typography>OAuthButton</Typography>
    </Container>
  );
}